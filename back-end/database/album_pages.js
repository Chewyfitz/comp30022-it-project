/*Name of the collections the database stores*/
const albumPagesCollection = 'Album Pages';

function addAlbumPage(userID, albumName, pageNumber) {

}

module.exports = {
    albumPagesCollection: albumPagesCollection,
    addAlbumPage: function (userID, albumName, pageNumber) {addAlbumPage(userID, albumName, pageNumber);},
}