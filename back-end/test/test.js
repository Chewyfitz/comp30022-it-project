/*
// This is a sample file to show the structure of testing files for reference while they are being created
var assert = require('assert');

describe('Array', function() {
    describe('#indexof()', function () {
        // pending test
        it('should return -1 when the value is not present');
        // normal test
        it('should return the correct index for an element in the array', function(){
            const array = [1,2,3]
            assert.equal(array.indexOf(1), 0);
            assert.equal(array.indexOf(2), 1);
            assert.equal(array.indexOf(3), 2);
        })
    });
});
*/